# ── FitVision Backend — Lean Deploy Requirements ──────────────────────────────
# Only what the FastAPI /predict endpoints actually need at runtime.
# Heavy packages (torch, mediapipe, ultralytics, streamlit, etc.) are excluded.

# Web Framework
fastapi>=0.104.1
uvicorn>=0.24.0

numpy==1.26.4
scipy==1.15.3
joblib==1.5.2
scikit-learn==1.3.2
xgboost==3.1.1

# Utils
pyyaml==6.0.1
